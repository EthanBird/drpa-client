# Hello Web Bot

这是一个最小脚本包示例，用于验证 DRPA Client 的安装、参数表单、任务运行、日志和输出文件能力。

打包方式：

```bash
cd examples/hello_web_bot
zip -r ../hello_web_bot.rpaz .
```

在 GUI 的“脚本包”页面选择 `examples/hello_web_bot.rpaz` 即可安装。
